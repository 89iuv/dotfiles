server {
    listen 11435;

    location / {
        # Authentication: require API key in Authorization header
        if ($http_authorization != "Bearer ${OLLAMA_API_KEY}") {
            return 401;
        }

        # Proxy to Ollama server
        proxy_pass http://localhost:11434;
        proxy_set_header Host localhost:11434;
    }
}
